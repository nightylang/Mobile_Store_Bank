# =========================================================================
# MOBILE STORE BANK - DEFINITIVE FULL INFRASTRUCTURE ORCHESTRATOR
# ARCHITECTURE: .NET 10.0 HTTP + MS SQL SERVER 2022 CONTAINER NETWORK
# =========================================================================

version: '3.8'

services:
  # LAYER 1: ENTERPRISE DATA PERSISTENCE LAYER (SQL SERVER)
  msb-sql-engine:
    image: ://microsoft.com
    container_name: msb_production_sql_node
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=SeniorDevPass_10.0_GA!
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    volumes:
      - msb-sql-data:/var/opt/mssql/data
    restart: always
    healthcheck:
      test: ["CMD", "/opt/mssql-tools/bin/sqlcmd", "-S", "localhost", "-U", "sa", "-P", "SeniorDevPass_10.0_GA!", "-Q", "SELECT 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # LAYER 2: HIGH-DENSITY WEB RUNTIME ENGINE (MVC APPLICATION)
  msb-web-platform:
    image: mobilestorebank-web:latest
    container_name: msb_production_http_node
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000" # Explicit cleartext port mapping hook
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:5000 # Enforce pure cleartext HTTP routing limits
      - ConnectionStrings__DefaultConnection=Server=msb-sql-engine;Database=MobileStoreBankDb;User Id=sa;Password=SeniorDevPass_10.0_GA!;TrustServerCertificate=True;MultipleActiveResultSets=True;
    restart: always
    depends_on:
      msb-sql-engine:
        condition: service_healthy

# =========================================================================
# SECURE STORAGE BOUNDARY VOLUMES
# =========================================================================
volumes:
  msb-sql-data:
    driver: local
    
